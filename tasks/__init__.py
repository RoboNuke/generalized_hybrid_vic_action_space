"""Repo-local Isaac Lab task definitions.

Tasks here are authored in this repository (not the upstream IsaacLab tree) but
register themselves into the same ``gymnasium`` registry as ``isaaclab_tasks``.
Import a subpackage (e.g. ``import tasks.flat_surface_follow``) to register its ids.
"""
